{
  "name": "@ai-interviewer/matching-service",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "check-types": "tsc --noEmit",
    "test": "bun test",
    "db:migrate": "echo 'matching-service shares the applications DB — run migrations in services/application-service' && exit 1",
    "db:generate": "bunx prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "prisma": "^7.8.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@ai-interviewer/api-types": "workspace:*",
    "@ai-interviewer/event-bus": "workspace:*",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "exports": {
    ".": "./index.ts",
    "./analyze": "./src/application/matching/match-analyzer.ts"
  }
}

